node_modules/
.next/
out/
build/
.DS_Store

# Local env — never commit.
# The broad .env.* catches backups like .env.neon-backup, which the narrower
# .env*.local missed — a credentials file one `git add -A` away from being
# committed. .env.example is the one that is meant to be tracked.
.env
.env.*
!.env.example

# Local SQLite database
prisma/dev.db
prisma/dev.db-journal
*.db
*.db-journal

next-env.d.ts
*.tsbuildinfo
.vercel
backup-pre-auth.json

# Design sources. The optimised derivatives in public/ are what ships;
# keeping multi-megabyte originals out of the repo keeps clones and deploys small.
design/
